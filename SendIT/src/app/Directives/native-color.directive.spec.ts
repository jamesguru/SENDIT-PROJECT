import { NativeColorDirective } from './native-color.directive';

describe('NativeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NativeColorDirective();
    expect(directive).toBeTruthy();
  });
});
